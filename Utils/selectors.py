from selenium.webdriver.support.ui import Select
from selenium.webdriver.common.alert import Alert

# You can wrap Selenium's classes if you need to customize their behavior
# For simple usage, it's often enough to use them directly in the page objects
